# Convex functions

This directory contains only clean-start MayLamDi functions.

`health.ts` is the foundation-gate query for the existing
`resilient-mastiff-759` development deployment. No QuestBoard functions were
restored.
